# Changelog

All notable changes are documented here. This project follows Semantic
Versioning once a public API release is approved.

## 0.1.0 — 2026-07-29

- Established the versioned CIR, processing-event, route, error, export, and
  knowledge contracts.
- Added native document parsing, deterministic routing and quality gates,
  provenance-preserving knowledge compilation, and four AKMP export profiles.
- Added the authenticated FastAPI control plane, tenant isolation, append-only
  credit operations, direct object-store upload flow, SSE replay, review,
  deletion receipts, signed webhooks, and durable job dispatch.
- Added the live Next.js product workspace, source/Markdown linkage, review and
  export flows, responsive layouts, CSP nonces, and browser tests.
- Added PostgreSQL RLS migrations, isolated scheduler roles, Kubernetes,
  Terraform, Runpod contracts, monitoring, runbooks, CI, security scanning,
  SBOM generation, and evidence-gated release workflows.
- Added hash-bearing Python and pnpm lockfiles, dependency security overrides,
  synthetic benchmark contracts, a 100-page native PDF Gate 1 journey, and
  fail-closed boundaries for every unconfigured external provider.
- Added immutable page-attempt evidence, append-only transition events,
  measured native quality/escalation, idempotent terminal-page retries, and a
  durable fail-closed visual-provider handoff with explicit waiting state.
- Added exact page-scoped 200/300 DPI inference rasters, strict structured
  visual result admission, mandatory confidence and independent verification,
  pre-persistence security review, candidate-only escalation, atomic accepted
  promotion that preserves user locks, and model-run-complete CIR/export
  reconstruction.

This release is a production-oriented implementation baseline, not a production
go-live assertion. Licensed corpus benchmarks, real GPU/provider results, legal
approval, payments, private beta evidence, production SLOs, and operational
drills remain external release gates.

# W1 · Hero 정적 시안 — 승인 완료

설계도 §12.2 · §25.1. 결정 3건 모두 2026-08-07 에 오너 판정으로 확정됐다.
정본 기록은 `../../decision.md` 의 G-F · G-G · G-H 다.

| 결정 | 결과 |
| --- | --- |
| G-F · Hero 구성 | **A · Frame** |
| G-G · Hero 카피 | **D1 · 근거 우선** |
| G-H · 액센트 색상각 | **255°** |

재생성 — dev 서버를 띄우고 `/design/hero?variant=<v>&copy=<c>` 를 캡처한다.
`variant` = `frame` · `overlap` · `fullbleed`, `copy` = `d1` · `d2` · `d3`.

---

## 승인안 · A · Frame + D1

```
approved-1440.png    approved-390.png
```

액센트 255° 확정 후 다시 캡처한 것이다. **이것이 W2 의 기준이다.**

## 보류안 (폐기 아님)

```
frame-d1-1440.png  frame-d1-390.png        승인안의 265° 시점 캡처
overlap-d1-*.png                            B · Overlap
fullbleed-d1-*.png                          C · Full-bleed verso
```

- **B · Overlap** — 위계는 가장 뚜렷하나 `--depth-overlay` 에 의존해 카드로
  읽힐 위험이 있다(§25.7 "Hero 에 카드" 금지에 근접). Knowledge(§12.5)에서
  재검토 여지가 있다.
- **C · Full-bleed verso** — "문서는 더 많다"는 인상이 강하다. 1920 이상에서
  우측 잘림 여백 규칙을 따로 정해야 한다. Proof(§12.4) 후보.

세 안의 차이는 구성뿐이다. 카피·데이터·좌표·타이포는 동일하게 유지했다.

---

## 시안이 담고 있는 것

- 좌측면은 **저장된 bbox1000 좌표대로 블록을 배치한** 페이지다. 흐름 배치가
  아니다 — 그래야 "Table 3" 하이라이트가 실제로 표 위에 놓인다.
- 스레드는 그 좌표에서 계산된다. 좌표가 없는 앵커는 선을 그리지 않는다 (§4.4).
- 390 세로 스택에서는 스레드를 **그리지 않는다.** 기하가 가로 배치 기준이라
  세로에서는 엉뚱한 위치에 닿는다. 텍스트 등가물은 유지된다.
- 수치는 전부 동결 fixture(`demo-data.ts`)에서 온다. 지어낸 값이 없다.

## 시안이 담고 있지 않은 것

- **동작하는 드롭존.** §12.2 의 상태 ②③ 은 W2 다. CTA 는 `<span>` 으로 두어
  §14.3 의 죽은 컨트롤을 만들지 않았다.
- **실제 PDF 렌더.** 좌측면은 페이지 facsimile 이고 "not an actual source"
  라고 명시한다. PDF.js 는 W4 에서 G-E 좌표 계약과 함께 들어온다.
- 모션. §12.2 의 5구간 시퀀스는 승인된 1안에 대해 W2 에서 구현한다.

apiVersion: v1
kind: ServiceAccount
metadata:
  name: akc-gpu-worker
automountServiceAccountToken: false
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: akc-gpu-worker
  labels:
    app.kubernetes.io/name: akc-gpu-worker
    app.kubernetes.io/component: gpu-provider-control-worker
spec:
  replicas: 2
  minReadySeconds: 10
  progressDeadlineSeconds: 600
  revisionHistoryLimit: 5
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: akc-gpu-worker
  template:
    metadata:
      labels:
        app.kubernetes.io/name: akc-gpu-worker
        app.kubernetes.io/component: gpu-provider-control-worker
    spec:
      serviceAccountName: akc-gpu-worker
      automountServiceAccountToken: false
      enableServiceLinks: false
      terminationGracePeriodSeconds: 180
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile:
          type: RuntimeDefault
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: akc-gpu-worker
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: akc-gpu-worker
      containers:
        - name: gpu-worker
          # An environment overlay MUST replace this with a signed immutable digest.
          image: registry.invalid/akc-scheduler:replace-with-signed-digest
          imagePullPolicy: IfNotPresent
          command: [python, -m, akc_scheduler, --mode, gpu]
          ports:
            - name: metrics
              containerPort: 9104
              protocol: TCP
          env:
            - name: HOME
              value: /tmp
            - name: AWS_EC2_METADATA_DISABLED
              value: "true"
          envFrom:
            - configMapRef:
                name: akc-gpu-worker-runtime
            - secretRef:
                name: akc-gpu-worker-secrets
                optional: false
          startupProbe:
            exec:
              command: [python, -m, akc_scheduler, --check, --mode, gpu]
            periodSeconds: 5
            timeoutSeconds: 10
            failureThreshold: 30
          readinessProbe:
            exec:
              command: [python, -m, akc_scheduler, --check, --mode, gpu]
            periodSeconds: 15
            timeoutSeconds: 10
            failureThreshold: 3
          livenessProbe:
            exec:
              command: [python, -c, "import os; os.kill(1, 0)"]
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              cpu: 200m
              memory: 256Mi
              ephemeral-storage: 64Mi
            limits:
              cpu: "1"
              memory: 1Gi
              ephemeral-storage: 256Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: [ALL]
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir:
            sizeLimit: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: akc-gpu-worker-metrics
  labels:
    app.kubernetes.io/name: akc-gpu-worker
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: akc-gpu-worker
  ports:
    - name: metrics
      port: 9104
      targetPort: metrics
      protocol: TCP
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: akc-gpu-worker
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: akc-gpu-worker
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: monitoring-to-gpu-worker
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: akc-gpu-worker
  policyTypes: [Ingress]
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: monitoring
          podSelector:
            matchLabels:
              app.kubernetes.io/name: prometheus
      ports:
        - protocol: TCP
          port: 9104

# Benchmark Reports

Generated reports and raw artifacts belong in immutable CI/release storage.
Only this policy file is committed. Local report output must be labeled
`contract_test`; production promotion consumes signed `internal_result`
artifacts and their corpus/model/environment manifests.

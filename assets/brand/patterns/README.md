# Deterministic pattern system

`FolyntaPattern` implements the six first-party vector patterns:
Page Grid, Semantic Blocks, Evidence Paths, Node Constellation, Coordinate
Field, and Compilation Layers. Patterns inherit the current evidence color and
remain decorative, non-semantic, and responsive.
